<?xml version="1.0" encoding="utf-8" ?>

<ContentPage
    x:Class="Task_Manager.Views.Tasks.TasksPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="{Binding Title}"
    BackgroundColor="White">

    <ScrollView>

        <VerticalStackLayout
            Padding="20"
            Spacing="16">

            <Entry
                Placeholder="Task Title"
                Text="{Binding TaskTitle}"/>

            <Editor
                Placeholder="Description"
                AutoSize="TextChanges"
                HeightRequest="100"
                Text="{Binding Description}"/>

            <DatePicker
                Date="{Binding DueDate}"/>

            <Picker
                x:Name="PriorityPicker"
                Title="Priority"
                SelectedItem="{Binding Priority}"/>

            <Button
                Text="Add Task"
                Command="{Binding AddTaskCommand}"/>

            <CollectionView
                ItemsSource="{Binding Tasks}">

                <CollectionView.EmptyView>

                    <VerticalStackLayout
                        Padding="30">

                        <Label
                            HorizontalOptions="Center"
                            Text="No tasks yet."/>

                    </VerticalStackLayout>

                </CollectionView.EmptyView>

                <CollectionView.ItemTemplate>

                    <DataTemplate>

                        <SwipeView>

                            <SwipeView.RightItems>
                                <SwipeItems>
                                    <SwipeItem
                                        Text="Delete"
                                        BackgroundColor="Red"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteTaskCommand}"
                                        CommandParameter="{Binding .}"/>
                                </SwipeItems>
                            </SwipeView.RightItems>

                            <Frame
                                Margin="0,5"
                                Padding="15"
                                BorderColor="LightGray">

                                <Grid ColumnDefinitions="Auto,*,Auto" RowDefinitions="Auto,Auto,Auto,Auto" ColumnSpacing="10">

                                    <CheckBox
                                        Grid.Row="0"
                                        Grid.Column="0"
                                        Grid.RowSpan="4"
                                        VerticalOptions="Center"
                                        IsChecked="{Binding IsCompleted}"
                                        CheckedChanged="OnTaskCheckedChanged"/>

                                    <Label
                                        Grid.Row="0"
                                        Grid.Column="1"
                                        FontAttributes="Bold"
                                        FontSize="18"
                                        Text="{Binding Title}"/>

                                    <Label
                                        Grid.Row="1"
                                        Grid.Column="1"
                                        Text="{Binding Description}"
                                        TextColor="Gray"/>

                                    <HorizontalStackLayout
                                        Grid.Row="2"
                                        Grid.Column="1"
                                        Spacing="10">

                                        <Label
                                            FontSize="12"
                                            Text="{Binding DueDate, StringFormat='Due: {0:MMM dd, yyyy}'}"/>

                                        <Label
                                            FontSize="12"
                                            FontAttributes="Bold"
                                            Text="OVERDUE"
                                            TextColor="Red"
                                            IsVisible="{Binding IsOverdue}"/>

                                    </HorizontalStackLayout>

                                    <Label
                                        Grid.Row="3"
                                        Grid.Column="1"
                                        FontSize="12"
                                        FontAttributes="Bold"
                                        Text="{Binding Priority, StringFormat='Priority: {0}'}"/>

                                    <Button
                                        Grid.Row="0"
                                        Grid.Column="2"
                                        Grid.RowSpan="4"
                                        Text="Edit"
                                        VerticalOptions="Center"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EditTaskCommand}"
                                        CommandParameter="{Binding .}"/>

                                </Grid>

                            </Frame>

                        </SwipeView>

                    </DataTemplate>

                </CollectionView.ItemTemplate>

            </CollectionView>

        </VerticalStackLayout>

    </ScrollView>

</ContentPage>